ADMIN_API_KEY=egymac123@

# Persistent storage on Vercel (required for data to survive refreshes):
# 1. Vercel Dashboard → your project → Storage → Create → Blob
# 2. Connect the Blob store to this project (Production + Preview)
# 3. Redeploy — health check will show "persistent": true
